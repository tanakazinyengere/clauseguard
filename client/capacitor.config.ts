import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tanakazinyengere.clauseguard',
  appName: 'ClauseGuard',
  webDir: 'dist',
};

export default config;
